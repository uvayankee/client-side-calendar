{
  "name": "calendar-project",
  "version": "1.0.0",
  "description": "A simple calendar application",
  "main": "dist/index.js",
  "scripts": {
    "build": "mkdir -p dist && esbuild src/main.ts --bundle --format=iife --outfile=dist/index.js --sourcemap && cp src/index.html dist/ && cp src/style.css dist/",
    "test": "jest",
    "lint": "eslint . --ext .ts",
    "serve": "npx http-server dist",
    "cy:run": "cypress run"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@typescript-eslint/eslint-plugin": "^8.39.0",
    "@typescript-eslint/parser": "^8.39.0",
    "cypress": "^14.5.4",
    "esbuild": "^0.25.8",
    "eslint": "^9.33.0",
    "http-server": "^14.1.1",
    "husky": "^9.1.7",
    "jest": "^30.0.5",
    "jest-environment-jsdom": "^30.0.5",
    "ts-jest": "^29.4.1",
    "typescript": "^5.9.2"
  }
}
